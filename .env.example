# Optional: enables durable PostgreSQL persistence. Without it, the app uses
# temporary in-memory storage that can reset between serverless invocations.
DATABASE_URL=

# Optional: enables OpenAI-powered chat. Without it, NephroBot uses the
# repository's built-in educational kidney-health responses.
OPENAI_API_KEY=
